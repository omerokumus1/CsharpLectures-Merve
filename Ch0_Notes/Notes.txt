- Console.WriteLine'nın ağır operasyon olması
- char c5 = Convert.ToChar(5); -> neden '5' çıkartmıyor?
                int i5 = Convert.ToInt32('4'); -> neden 52 çıkartıyor?

- Tek satır if içerisinde neden değişken declare edilemez

- GuessingNumber'ı do-while ile yaz

- Next: char type, data types and storages, reference, var keyword
- u -> unsigned
    s -> signed
    '\''
    "\""
    x as double
    x is double
    C# kitabı: 
        + Data types:
            - Types
            - Real-floating point Types
            - Real types with decimal Precision
            - Character Type (ASCII table)
            - Value and Reference Types
            - null, nullable types
            - Operators as, is, typeof, ??, sizeof
            - Overflow, Data loss 

- String döngü ödevleri:
    + Capitalize (baş harfi büyük yap)
    + Add period (sonuna nokta koy)
    + Count punctuations
    + Count turkish letters
    + Count uppercase letters
    + Count words
    + ToUpper
    + ToLower
    + Capitalize each word
    + Capitalize turkish letters, lower others
    + Clone
    + Contains character
    - Contains string** 
    + Substring
    + StartsWith
    + EndsWith
    + IndexOf
    + LastIndexOf
    + Equals
    + Insert character
    + Insert string**
    + Remove all characters**
    + Remove a character as given number of times (2 tane sil, 5 tane sil vb)**
    + Remove a character starting from a given index as given number of times (2. indexten başla 3 tane sil gibi)***
    + Remove a character at an index
    - Remove string** 
    + Replace all characters**
    + Replace a character as given number of times (2 tane değiştir, 5 tane değiştir vb)**
    + Replace a character starting from a given index as given number of times (2. indexten başla 3 tane değiştir gibi)***
    + Replace a character at an index with another given character
    - Replace string**
    + Trim spaces
    + Trim end
    + Trim start
    - CompareTo**
    - Pad tab
    - Pad left
    - Pad right
   
    